'use client'
export default function UpdateUser({ params }: { params: { id: string } }) {
  return (
    <div>
      {/* Your UpdateUser component content */}
      {/* Access ID using params.id */}
    </div>
  );
} 